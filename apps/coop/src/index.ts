// Database layer only so far (schema in migrations/0001_coop.sql). Members,
// loans, and dividend application logic land in later phases — see
// .claude/skills/ajo-cooperative/SKILL.md.
export {};
