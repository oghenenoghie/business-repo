// Database layer only so far (schema in migrations/0001_school.sql). The
// timetable solver, CBT exam engine, and fee application logic land in
// later phases — see .claude/skills/termly-school/SKILL.md.
export {};
