// Database layer only so far (schema in migrations/0001_hotel.sql).
// Availability search, front desk, night audit, and channel sync land in
// later phases — see .claude/skills/portier-hotel/SKILL.md.
export {};
